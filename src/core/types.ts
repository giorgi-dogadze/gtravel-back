export interface User {
  id: string;
  slug: string;
  email: string;
  passwordHash: string;
  name: string;
  lastName: string;
  personalNumber: string;
  phone: string;
  createdAt: string;
  updatedAt: string;
}
